
    <link rel="icon" type="image/png" href="${base}/static/images/icon.png">
    <#import "../loader.ftl" as loader>

    <#include "../libraries/moment.ftl">
    <#include "../libraries/bootstrap.ftl">
    <#include "../libraries/angular.ftl">

    <!-- Here so it can override bootstrap's default style -->
    <link type="text/css" rel="stylesheet" href="${base}/static/css/dashboard.css">
